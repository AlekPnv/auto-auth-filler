# Local OAuth configuration. Copy config.template.js to config.js and put your
# own Client ID there. The template is committed; config.js never is.
config.js

# Packaging output
dist/
dist-chrome/
*.zip

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo

# Personal working notes. Kept on disk, not in the repository.
DEMO_VIDEO.md
SUBMISSION.md
STORE_LISTING.md

# Store submission working files
store-assets/

# Local media. The demo MP4 is 52 MB; GitHub warns past 50 and blocks at 100,
# and a binary that size can never be removed from history cleanly.
Public Media/
